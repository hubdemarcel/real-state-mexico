name: Deploy to Hostinger

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Deploy to Hostinger via FTP
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ inteligencias.info }}
        username: ${{ u453889947.inteligencias.info }}
        password: ${{ Host2024db! }}
        local-dir: ./
        server-dir: ./public_html/
        exclude: |
          .git*
          .github*
          *.md
          DEPLOYMENT_FIX.md
